<?PHP

// FILE INCLUDE VALIDATION
if (!defined('IDEV_EMAIL')) { die('Unauthorized Access'); }
// -------------------------------------------------------------------------------------------------

include_once($path . "/templates/email/PHPMailerAutoload.php");
use PHPMailer\PHPMailer\PHPMailer;
$mail = new PHPMailer();
$mail->SMTPAutoTLS = false;

if ($email_smtp_delivery == true) {
$mail->IsSMTP();
	if ( $smtp_auth == 'true' ) {
		$mail->SMTPAuth = true;
	} else {
		$mail->SMTPAuth = false;
	}
$mail->SMTPSecure = "$smtp_security";
$mail->Host = "$smtp_host";
$mail->Port = $smtp_port;
$mail->Username = "$smtp_user";
$mail->Password = "$smtp_pass"; }
$mail->CharSet = "$smtp_char_set";

if ($email_html_delivery == true) {
$mail->isHTML(true);
$content = "A commission could not be generated by the PayPal IPN system.  We could not establish a connection with the PayPal server to verify the payment.<br /><br />Make sure you have one of the following enabled on your server.<br />- cURL<br />- fsockopen<br /><br />Please consult your hosting provider and/or server admin for help with these server settings.<br /><br />--------<br />Message Auto-Sent By Sublime Revenue " . $version;
} else {
$mail->isHTML(false);
$content = "A commission could not be generated by the PayPal IPN system.  We could not establish a connection with the PayPal server to verify the payment.\n\nMake sure you have one of the following enabled on your server.\n- cURL\n- fsockopen\n\nPlease consult your hosting provider and/or server admin for help with these server settings.\n\n--------\nMessage Auto-Sent By Sublime Revenue " . $version;
}

$mail->Subject = "Sublime Revenue Commission Processing Error";
$mail->From = "$address";
$mail->FromName = "Sublime Revenue Mailbox";
$mail->AddAddress("$address","Sublime Revenue Mailbox");

// Added for multiple CC's, semi-colon separated.
if($cc_email == true) {
	$cc_emails = explode(";", $cc_email_address);
	foreach($cc_emails as $added_cc_emails)
	{
		$added_cc_emails_stripped = str_replace(' ', '', $added_cc_emails);
		$mail->AddCC($added_cc_emails_stripped,"Sublime Revenue Admin");
	}
}

$mail->Body = $content;

$mail->Send();
$mail->ClearAddresses();


?>